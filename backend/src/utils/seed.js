require('dotenv').config();const bcrypt=require('bcrypt');const db=require('../config/db');async function main(){const passwordHash=await bcrypt.hash('password123',10);const user=await db.query(`INSERT INTO users (email,password_hash,public_name,role,plan) VALUES ($1,$2,$3,$4,$5) ON CONFLICT (email) DO UPDATE SET public_name=EXCLUDED.public_name RETURNING *`,['demo@partagetamusique.com',passwordHash,'Demo Creator','admin','pro']);const artist=await db.query(`INSERT INTO artists (owner_user_id,public_name,bio) VALUES ($1,$2,$3) RETURNING *`,[user.rows[0].id,'NOVA INDÉ','Artiste indépendant de démonstration']);await db.query(`INSERT INTO tracks (artist_id,title,genre,description,audio_url,rights_confirmed,no_unauthorized_samples) VALUES ($1,$2,$3,$4,$5,true,true)`,[artist.rows[0].id,'Bruxelles Minuit','Rap','Son de démonstration légal','/demo/bruxelles-minuit.mp3']);console.log('Seed done');process.exit(0);}main().catch(err=>{console.error(err);process.exit(1);});